import PageStub from '../components/PageStub';
export default function Page() {
  return <PageStub title="Asset Inventory" description="Domains, subdomains, servers, technologies, and dependencies with tagging and grouping." />;
}
