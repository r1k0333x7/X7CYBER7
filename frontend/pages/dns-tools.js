import PageStub from '../components/PageStub';
export default function Page() {
  return <PageStub title="DNS Tools" description="A, AAAA, MX, TXT, NS, CNAME lookups, reverse DNS, and DNS-over-HTTPS queries." />;
}
